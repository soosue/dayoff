package com.project.justdo.domain.repository;

import com.project.justdo.domain.DayOffApplication;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DayOffApplicationRepository extends JpaRepository<DayOffApplication, Long> {
}
